export const Config = {
  // dynamicLinksUrlPrefix: 'steplerdevelopment', // Should be unique string, only letters
  // inviteFallbackUrl: 'https://stepler.se', // Should point to real page to avoid bad UX
  apiEndpoint: 'https://api.invoice-app.2muchcoffee.com/api/',
  // apiWSEndpoint: 'wss://stepler-api.2mc.team/graphql',
  // locales: ['en', 'sv'],
  // defaultLocale: 'en',
};
